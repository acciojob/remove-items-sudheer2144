//your JS code here. If required.
function remove(){

  let elements=document.getElementById("colorSelect");

  // console.log(elements.selectedIndex);
  elements.remove(elements.selectedIndex);

  // console.log(elements);
}